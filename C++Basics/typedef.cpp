#include <iostream>
using namespace std;

typedef int marks;
typedef int rollno;

int main()
{
    marks m1, m2, m3;   // Marks is a predefined data type that describes what type its variables are.
    rollno r1, r2, r3;  // Rollno is also a predefined data type that desctibes its variables. 

    // This can later be used in the code by some functions.
    // Typedef is used for the readability in the program, not for efficiency. 
    // Typedef defines data types that later define (succintly) their variables. 

    return 0;
}