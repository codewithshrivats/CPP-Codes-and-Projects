#include <iostream>
using namespace std;

int main()
{
    int a, b, c;

    /* if()
    {
        int i;  This is called dynamic declaration. As soon as the program enters this if block 
                variable i is assigned memory in the stack section (activation record of main function).
                When the program exits this code block, the variables are deleted. 
    }

    if()
    {
        int j;
    } 
    
    if(int i = 0, i < k)    This expression is also allowed. 
    {
        ...
    }

    */

    return 0;
}